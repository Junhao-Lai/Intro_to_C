#include <stdio.h>

int main()
{
  int i = 0;
  while(i<=10){
    i++;
    printf("%d\n",i);
  }

  if(c == ' '){
    ++space; 
  }else if(c >= '0' && c <= '9'){
    ++digit;
  }else if(c >= 'a' && c <= 'z' || c >= 'A' && c <= 'Z'){
    ++letter;
  }else{
    ++other;
  }
    
  
}
